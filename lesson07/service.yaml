---
kind: Service
apiVersion: v1
metadata:
  name: prometheus
  labels:
    app: prometheus
spec:
  clusterIP: None
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9090
  selector:
    app: prometheus